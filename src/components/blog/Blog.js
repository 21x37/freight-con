import React from 'react';
import Header from '../Header';

const Blog = () => {
    return (
        <div>
            <Header />
            <p>Blog page.</p>
        </div>
    );
};

export default Blog;